{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt && python vercel_build.py",
  "builds": [
    {
      "src": "gbaki_backend/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.12"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "gbaki_backend/wsgi.py"
    }
  ]
}